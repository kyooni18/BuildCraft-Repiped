pluginManagement {
    repositories {
        maven { url = uri('/mnt/data/bcenv/offline_maven') }
        gradlePluginPortal()
        maven {
            name = 'MinecraftForge'
            url = 'https://maven.minecraftforge.net/'
        }
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'BuildCraft'
