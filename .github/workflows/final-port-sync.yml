name: Final NeoForge port sync

on:
  push:
    branches:
      - 1.21.1-NeoForge
    paths:
      - .github/workflows/final-port-sync.yml

permissions:
  contents: write

jobs:
  idle:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Payload staging mode"
